{
  "name": "gallery-block",
  "version": "1.3.0",
  "description": "Standalone gallery block filtered by taxonomy or category",
  "scripts": {
    "build": "wp-scripts build src/index.js src/view.js --output-path=build",
    "postbuild": "cp src/block.json build/block.json && cp src/style.css build/style.css && cp src/index.css build/index.css",
    "start": "wp-scripts start src/index.js src/view.js --output-path=build",
    "format": "wp-scripts format",
    "lint:css": "wp-scripts lint-style",
    "lint:js": "wp-scripts lint-js"
  },
  "devDependencies": {
    "@wordpress/scripts": "^27.0.0"
  }
}
